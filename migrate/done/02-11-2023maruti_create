ALTER TABLE `cto_cashier` ADD `payment_taransaction_id` VARCHAR(255) NULL AFTER `is_collected`, ADD `merchant` TEXT NULL AFTER `payment_taransaction_id`, ADD `txnstatus` INT(11) NULL DEFAULT '0' AFTER `merchant`, ADD `paymentid` VARCHAR(255) NULL AFTER `txnstatus`, ADD `transactiontype` VARCHAR(255) NULL AFTER `paymentid`, ADD `authcode` VARCHAR(255) NULL AFTER `transactiontype`, ADD `txnmessage` TEXT NULL AFTER `authcode`, ADD `token` TEXT NULL AFTER `txnmessage`, ADD `tokentype` VARCHAR(255) NULL AFTER `token`, ADD `cardholder` VARCHAR(255) NULL AFTER `tokentype`, ADD `issuingbank` VARCHAR(255) NULL AFTER `cardholder`, ADD `cardnomask` VARCHAR(255) NULL AFTER `issuingbank`, ADD `cardexp` VARCHAR(200) NULL AFTER `cardnomask`, ADD `cardtype` VARCHAR(255) NULL AFTER `cardexp`, ADD `settletaid` VARCHAR(255) NULL AFTER `cardtype`, ADD `tid` VARCHAR(255) NULL AFTER `settletaid`, ADD `currencycode` VARCHAR(255) NULL AFTER `tid`;


ALTER TABLE `bplo_bill_summary` ADD `payment_taransaction_id` VARCHAR(255) NULL AFTER `is_synced`, ADD `merchant` TEXT NULL AFTER `payment_taransaction_id`, ADD `txnstatus` INT(11) NULL DEFAULT '0' AFTER `merchant`, ADD `paymentid` VARCHAR(255) NULL AFTER `txnstatus`, ADD `transactiontype` VARCHAR(255) NULL AFTER `paymentid`, ADD `authcode` VARCHAR(255) NULL AFTER `transactiontype`, ADD `txnmessage` TEXT NULL AFTER `authcode`, ADD `token` TEXT NULL AFTER `txnmessage`, ADD `tokentype` VARCHAR(255) NULL AFTER `token`, ADD `cardholder` VARCHAR(255) NULL AFTER `tokentype`, ADD `issuingbank` VARCHAR(255) NULL AFTER `cardholder`, ADD `cardnomask` VARCHAR(255) NULL AFTER `issuingbank`, ADD `cardexp` VARCHAR(200) NULL AFTER `cardnomask`, ADD `cardtype` VARCHAR(255) NULL AFTER `cardexp`, ADD `settletaid` VARCHAR(255) NULL AFTER `cardtype`, ADD `tid` VARCHAR(255) NULL AFTER `settletaid`, ADD `currencycode` VARCHAR(255) NULL AFTER `tid`;
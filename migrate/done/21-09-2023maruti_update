ALTER TABLE `cto_cashier_details` ADD `cemetery_application_id` INT(11) NULL COMMENT 'cemetery application id' AFTER `or_no`, ADD `total_amount` DOUBLE NULL COMMENT 'cemetory total amount' AFTER `cemetery_application_id`, ADD `paid_amount` DOUBLE NULL AFTER `total_amount`, ADD `remaining_balance` DOUBLE NULL AFTER `paid_amount`, ADD `status` INT(11) NULL DEFAULT '0' COMMENT 'cemetory payment status' AFTER `remaining_balance`;
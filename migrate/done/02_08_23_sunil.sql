ALTER TABLE `cto_cashier_real_properties` ADD `additional_credit_amount` DECIMAL(20,3) NULL AFTER `tax_credit_amount`;
ALTER TABLE `cto_cashier_real_properties` ADD `sc_covered_year` VARCHAR(4) NULL AFTER `is_short_collection`;
ALTER TABLE `cto_cashier_real_properties`  ADD `is_short_collection` TINYINT(1) NOT NULL DEFAULT '0' COMMENT '1 = Yes, 0 = No'  AFTER `previous_cashier_id`,  ADD `basic_amount` DECIMAL(20,3) NULL COMMENT 'New value after genearal revision to calculate short collection'  AFTER `is_short_collection`,  ADD `basic_discount_amount` DECIMAL(20,3) NULL COMMENT 'New value after genearal revision to calculate short collection'  AFTER `basic_amount`,  ADD `basic_penalty_amount` DECIMAL(20,3) NULL COMMENT 'New value after genearal revision to calculate short collection'  AFTER `basic_discount_amount`,  ADD `sef_amount` DECIMAL(20,3) NULL COMMENT 'New value after genearal revision to calculate short collection'  AFTER `basic_penalty_amount`,  ADD `sef_discount_amount` DECIMAL(20,3) NULL COMMENT 'New value after genearal revision to calculate short collection'  AFTER `sef_amount`,  ADD `sef_penalty_amount` DECIMAL(20,3) NULL COMMENT 'New value after genearal revision to calculate short collection'  AFTER `sef_discount_amount`,  ADD `sh_amount` DECIMAL(20,3) NULL COMMENT 'New value after genearal revision to calculate short collection'  AFTER `sef_penalty_amount`,  ADD `sh_discount_amount` DECIMAL(20,3) NULL COMMENT 'New value after genearal revision to calculate short collection'  AFTER `sh_amount`,  ADD `sh_penalty_amount` DECIMAL(20,3) NULL COMMENT 'New value after genearal revision to calculate short collection'  AFTER `sh_discount_amount`,  ADD `is_short_collection_paid` TINYINT(1) NOT NULL DEFAULT '0' COMMENT '1 = Yes, 0 = No'  AFTER `sh_penalty_amount`;
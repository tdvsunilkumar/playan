ALTER TABLE `eng_job_requests` ADD `is_synced` INT(11) NULL DEFAULT '0' AFTER `ejr_or_no`;
ALTER TABLE `eng_job_request_requirements` ADD `is_synced` INT(11) NULL DEFAULT '0' AFTER `req_id`;

ALTER TABLE `eng_job_requests` ADD `topno` VARCHAR(255) NULL AFTER `ejr_or_no`, ADD `orno` VARCHAR(255) NULL AFTER `topno`, ADD `ordate` DATE NULL AFTER `orno`, ADD `cashieramount` DOUBLE(10,2) NULL AFTER `ordate`;
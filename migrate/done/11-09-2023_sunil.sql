ALTER TABLE `cto_accounts_receivables` ADD `effectivity_year` VARCHAR(4) NULL COMMENT 'Reference Table rpt_properties.rp_app_effectivity' AFTER `pcs_id`;
ALTER TABLE `cto_accounts_receivables` ADD `rp_basic_interest` DECIMAL(20,3) NULL AFTER `rp_basic_amount`, ADD `rp_basic_discount` DECIMAL(20,3) NULL AFTER `rp_basic_interest`;
ALTER TABLE `cto_accounts_receivables` ADD `rp_sef_interest` DECIMAL(20,3) NULL AFTER `rp_sef_amount`, ADD `rp_sef_discount` DECIMAL(20,3) NULL AFTER `rp_sef_interest`;
ALTER TABLE `cto_accounts_receivables` ADD `rp_sht_interest` DECIMAL(20,3) NULL AFTER `rp_sht_amount`, ADD `rp_sht_discount` DECIMAL(20,3) NULL AFTER `rp_sht_interest`;
ALTER TABLE `cto_accounts_receivables` CHANGE `ar_control_no` `ar_control_no` VARCHAR(100) NOT NULL COMMENT 'Combination of(ay_year-ar_no)';
ALTER TABLE `cto_accounts_receivables` ADD `last_updated` DATE NULL COMMENT 'This column used to check when last time data updated to avoid duplicate data this column is applicable to cron job functions\r\n' AFTER `status`;
ALTER TABLE `cto_accounts_receivable_details` ADD `last_updated` DATE NULL COMMENT 'This column used to check when last time data updated to avoid duplicate data this column is applicable to cron job functions\r\n' AFTER `status`
ALTER TABLE `cto_accounts_receivables` CHANGE `rp_basic_amount` `outstand_basic_amount` DOUBLE(14,5) NOT NULL COMMENT 'Tax, Fees & Other Charges', CHANGE `rp_basic_interest` `outstand_basic_interest` DECIMAL(20,3) NULL DEFAULT NULL, CHANGE `rp_basic_discount` `outstand_basic_discount` DECIMAL(20,3) NULL DEFAULT NULL, CHANGE `rp_sef_amount` `outstand_sef_amount` DOUBLE(14,5) NOT NULL COMMENT 'Tax, Fees & Other Charges', CHANGE `rp_sef_interest` `outstand_sef_interest` DECIMAL(20,3) NULL DEFAULT NULL, CHANGE `rp_sef_discount` `outstand_sef_discount` DECIMAL(20,3) NULL DEFAULT NULL, CHANGE `rp_sht_amount` `outstand_sht_amount` DOUBLE(14,5) NOT NULL COMMENT 'Tax, Fees & Other Charges', CHANGE `rp_sht_interest` `outstand_sht_interest` DECIMAL(20,3) NULL DEFAULT NULL, CHANGE `rp_sht_discount` `outstand_sht_discount` DECIMAL(20,3) NULL DEFAULT NULL;
ALTER TABLE `cto_accounts_receivables` ADD `delinq_basic_amount` DECIMAL(20,3) NULL AFTER `outstand_sht_discount`, ADD `delinq_basic_interest` DECIMAL(20,3) NULL AFTER `delinq_basic_amount`, ADD `delinq_basic_discount` DECIMAL(20,3) NULL AFTER `delinq_basic_interest`, ADD `delinq_sef_amount` DECIMAL(20,3) NULL AFTER `delinq_basic_discount`, ADD `delinq_sef_interest` DECIMAL(20,3) NULL AFTER `delinq_sef_amount`, ADD `delinq_sef_discount` DECIMAL(20,3) NULL AFTER `delinq_sef_interest`, ADD `delinq_sht_amount` DECIMAL(20,3) NULL AFTER `delinq_sef_discount`, ADD `delinq_sht_interest` DECIMAL(20,3) NULL AFTER `delinq_sht_amount`, ADD `delinq_sht_discount` DECIMAL(20,3) NULL AFTER `delinq_sht_interest`;